{% extends 'base.html.twig' %}

{% block title %}{{parent()}} | Verre{% endblock %}

{% block body %}
    <h1>Verre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ verre.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ verre.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_verre_index') }}">back to list</a>

    <a href="{{ path('app_verre_edit', {'id': verre.id}) }}">edit</a>

    {{ include('verre/_delete_form.html.twig') }}
{% endblock %}
