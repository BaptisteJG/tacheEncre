<form method="post" action="{{ path('app_verre_delete', {'id': verre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ verre.id) }}">
    <button class="btn"><i class="fa-regular fa-trash-can text-danger"></i></button>
</form>
