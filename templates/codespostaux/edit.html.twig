{% extends 'base.html.twig' %}

{% block title %}{{parent()}} | Modier le codes postal{% endblock %}

{% block body %}
    <h1>Modier le codes postal</h1>

    <a href="{{ path('app_codespostaux_index') }}" class="btn btn-secondary">Retour à la liste</a>

    {{ include('codespostaux/_form.html.twig', {'button_label': 'Modifier'}) }}

{% endblock %}
