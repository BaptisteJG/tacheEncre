<form method="post" action="{{ path('app_types_cadres_delete', {'id': types_cadre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ types_cadre.id) }}">
    <button class="btn"><i class="fa-regular fa-trash-can text-danger"></i></button>
</form>
