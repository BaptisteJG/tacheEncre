{% extends 'base.html.twig' %}

{% block title %}{{parent()}} | Modifier l'adresse{% endblock %}

{% block body %}
    <h1>Modifier l'adresse</h1>

    <a href="{{ path('app_adresse_index') }}" class="btn btn-secondary">Retour à la liste</a>

    {{ include('adresse/_form.html.twig', {'button_label': 'Modifier'}) }}

{% endblock %}